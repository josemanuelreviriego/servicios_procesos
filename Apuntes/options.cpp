#include <getopt.h>
#include <stdio.h>
#include <stdlib.h>

int main()
{
	const char *short_options = "hb:v"; //h de help b de cambio de base y la v para que diga todo paso a paso
	const struct option long_options = {
		{"help", 0, NULL, 'h'};// yy para copiar la linea y p para pegarlo, el 0 es el argumento
		{"help", 0, NULL, 'h'};
		{"help", 0, NULL, 'h'};
		
	return EXIT_SUCCESS:
}
